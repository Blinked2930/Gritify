# Vibe Coding: Implementation Phases

## Phase 1: The "Grit" Shell (Days 1-2)
- Setup Next.js + Clerk + Convex.
- Build the `CircularProgress` component (Shadcn + Framer Motion).
- Implement the 2 AM reset logic in `convex/schema.ts`.

## Phase 2: Social & Sync (Days 3-4)
- Create the "Live Dashboard" with Convex subscriptions.
- Build the "Vouch" modal for backfilled days.
- Set up the HTTP Action for the external workout app integration.

## Phase 3: The "Wrapped" Engine (Days 5-7)
- Integrate OpenAI API.
- Create the "Wrapped" slide deck component.
- Build the "Legacy Onboarding" for the ~7 days you've already completed (Bulk entry logic).

## Phase 4: Portfolio Polish
- Implement `DemoProvider.tsx` to toggle between real data and sandbox data.
- Final cutesy "vibe" polish (haptic feedback, bouncy buttons).